# Code Compilation Agent Requirements

# Core dependencies
subprocess32>=3.5.4; python_version < '3.3'
psutil>=5.9.0
pathlib2>=2.3.7; python_version < '3.4'
python-dotenv>=1.0.0

# Build and compilation tools
setuptools>=65.0.0
wheel>=0.38.0
pip>=22.0.0

# Language-specific support
# Python
pylint>=2.15.0
black>=22.0.0
mypy>=0.991
pytest>=7.0.0
coverage>=6.5.0

# JavaScript/TypeScript (requires Node.js installed separately)
# These are managed via npm/yarn, not pip

# Java (requires JDK installed separately)
# Maven and Gradle are external tools

# File handling and utilities
pyyaml>=6.0
toml>=0.10.0
configparser>=5.3.0

# Async support
asyncio-subprocess>=0.1.2
aiofiles>=0.8.0

# Logging and monitoring
structlog>=22.1.0
colorlog>=6.7.0

# Error handling and retry
tenacity>=8.0.0
backoff>=2.2.0

# Process management
sh>=1.14.0

# Development dependencies
pytest-asyncio>=0.20.0
pytest-mock>=3.8.0
pytest-cov>=4.0.0
